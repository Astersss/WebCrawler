Full name:  _Linwei Chen_____________
SEAS login: _linweic_____________

Which features did you implement? 
  (list features, or write 'Entire assignment')
 ----MileStone 1 ----
1, a http client to communicate with localhost:8080
2, a web interface which present a form to let user enter xpath and html/xml url
3, when user enters information and submits the form, a response page will appear. If none of the queries matches the file, 
respones will indicate fail; if any one of the queries matches the file, response will indicate success and list all the matched queries.
4, unit test testing core functions of this assignment
5, xpath engines ignores whitespaces in xpath queries, except those in quotes
6, due to time limitation, xpath engine does not handle conditions where quotes appear in quotes
7, multiple xpath entered in the form box MUST be separated by semi colon 
8, xpath servlet is tested in Jetty
----MileStone 2 ----
9, a web interface to login, logout, signup
10, the relative path of web interface starts from /firstpage, using Jetty as test web container
11, display all the channels created by users
12, once login, one can create a channel and put xpath expresions to associate with the channel
13, display channels with channel name, last crawled time, url location, can raw content
14, crawler can handle both http and https link, follows the robots.txt, and can handle redirect response
15, not sure what a .xp file is, so I included a plain text file with the results of war and peace
16, no idea why the web page does not display exact xml document content, but in the log it can be displayed correctly

Did you complete any extra-credit tasks? If so, which ones?
  (list extra-credit tasks)
	no
Any special instructions for building and running your solution?
  (include detailed instructions, or write 'None')

Did you personally write _all_ the code you are submitting
(other than code from the course web page)?
  [* ] Yes
  [ ] No

Did you copy any code from the Internet, or from classmates?
  [ ] Yes
  [* ] No

Did you collaborate with anyone on this assignment?
  [ ] Yes
  [ *] No
